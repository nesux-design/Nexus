kind = "web"
previewPath = "/"
title = "Battle Royale"
version = "1.0.0"
id = "artifacts/battle-royale"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/battle-royale run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/battle-royale", "run", "build" ]
publicDir = "artifacts/battle-royale/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
